name : Tag Workflow

on: 
  workflow_dispatch:

env:
  a: 2
  a: 3


jobs:
  job1:
    runs-on : ubuntu-latest
    if: ${{ a > 0 && b > 0}}
    steps:
    - name: afficher les valeurs 
      run: |
        echo "a = ${{a}} et b = ${{b}}"
