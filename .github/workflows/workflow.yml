name : Tag Workflow

on: 
  workflow_dispatch:

jobs:
  job1:
    runs-on : ubuntu-latest
    env:
      a: 2
      b: 3
    if: ${{a}} > 0 && ${{b}} > 0
    steps:
    - name: afficher les valeurs 
      run: |
        echo "a = $a et b = $b"
